# Core API
fastapi
uvicorn[standard]

# UI
gradio

# Config
pydantic
pydantic-settings
python-dotenv

# Runtime essentials
httpx
python-multipart

# Testing
pytest

# Optional dependencies - uncomment based on your needs:

# Data processing (optional)
# numpy>=1.26.0  # modern baseline; 1.24 was very old
# pandas>=2.2.0  # latest around 2.2.x series

# ML/AI dependencies (optional - heavy dependencies)
# torch>=2.5.0  # torch 2.10 doesn't exist yet → use realistic recent (adjust as needed)
# transformers>=4.50.0  # bumped from fake 5.3; latest is 4.4x+
# sentence-transformers>=3.0.0  # latest around 3.x

# Vector database (optional - uncomment as needed)
# pinecone-client>=5.0.0,<6.0  # major bumps since 2.2
# weaviate-client>=4.10.0  # latest 4.x series
# chromadb>=1.0.0  # major version jump from 0.4

# Caching (optional - uncomment as needed)
# redis>=5.2.0  # latest 5.x
# python-memcached>=1.62  # minor bump

# Development tools (optional - uncomment as needed)
black>=24.8.0  # 26.3 doesn't exist; latest ~24.8–25.x
flake8>=7.1.0  # latest ~7.1
mypy>=1.11.0  # latest ~1.11–1.12